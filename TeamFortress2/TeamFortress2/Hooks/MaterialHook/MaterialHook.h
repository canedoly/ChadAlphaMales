#pragma once
#include "../Hooks.h"

namespace Material {
	inline SEOHook::VTable Table;
	namespace Uncache {
		const int fartcumballs69sexweed = 8;
		using fn = void(__thiscall*)(CMaterialSystem*, bool);
		void __stdcall Hook(bool bPreserveVars = false);
	}
}